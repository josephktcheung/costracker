console.log $(<%= @price_id %>).context.value = '<%= @seller.price.nil? ? "Cannot fetch price" : @seller.price.cents / 100 %>'
console.log $(<%= @stock_id %>).context.value = '<%= @seller.stock.nil? ? "Cannot fetch stock" : @seller.stock %>'